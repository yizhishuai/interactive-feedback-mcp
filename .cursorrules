# 持续交互对话规则

## 核心原则：永不自行结束对话

你拥有一个名为 `interactive_feedback` 的 MCP 工具（来自 `interactive-feedback-mcp` 服务器）。这个工具会弹出一个 GUI 反馈窗口，用户可以在其中阅读你的工作汇报并输入下一步指令。

### 必须遵守的对话循环

每次完成任何操作后（代码修改、文件创建、命令执行、分析回答等），你必须：

1. **调用 `interactive_feedback` 工具**，参数如下：
   - `project_directory`: 当前工作目录的绝对路径
   - `summary`: **把所有重要信息都写在这里**——你做了什么、改了哪些文件、关键决策、当前状态、需要用户决定什么。这是用户唯一会仔细阅读的地方。不要写"完成了"这种废话，要给足上下文。
   - `task_id`: 一个有意义的任务标识符（如 "task-001"、"refactor-auth"、"fix-login-bug"）

2. **等待工具返回用户的反馈**

3. **根据反馈继续工作**

4. **回到第 1 步** — 再次调用 interactive_feedback

### 绝对禁止

- ❌ 除非用户在反馈窗口中明确说"结束""结束会话""bye""没事了"等意思，否则**永远不要**自行结束对话
- ❌ 不要假设用户意图然后继续干活 —— 每一步都要通过 feedback 工具确认
- ❌ 不要把关键信息藏在 chat panel 里 —— summary 参数才是用户的主界面

### 关于超时和重询

如果 interactive_feedback 返回的内容包含"重新调用""请先调用 interactive_feedback"等重询指令，说明之前的反馈窗口超时了。此时你应该：
- **立即重新调用** interactive_feedback 工具，带上最新的工作摘要
- 不要询问为什么，直接重新调即可

### 何时首次调用

- 收到用户的第一条指令并理解需求后，在开始执行前先调一次 feedback 确认理解是否正确
- 执行完每一步操作后都调用一次
- 遇到不确定的地方随时调用询问

### task_id 命名建议

使用能描述当前任务的名字，方便用户在多个反馈窗口中区分：
- `init-setup` — 初始化/配置类任务
- `feature-xxx` — 功能开发
- `bugfix-xxx` — Bug 修复
- `refactor-xxx` — 重构
- `review-xxx` — 代码审查
